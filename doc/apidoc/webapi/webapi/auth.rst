Authorization and Authentication
================================

The API uses Basic Token Authentication for securing the endpoints. To access the API,
clients must include an `Authorization` header with a valid token in their requests.
